## v2.9.3 (2026-03-17) - Stop Unnecessary Sensor Data Push
Priority: low

Removed old real-time sensor push functionality to save bandwidth, as historical aggregations fully cover this data and latest reading provides the live updates.

### Changed
- `firebase.py`'s `sync_sensor_readings` is now completely deprecated to prevent excessive spam inside the `sensor_data` Firebase node.
